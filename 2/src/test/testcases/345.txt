main: function void() {
                    foo(2 + x, 4.0 / y);
                    goo();
                          for (i = 1, i <= (n * (n + 1)) / 2, i+1) {
                            printf("%d ", i);
                            if (i == (j * (j + 1)) / 2) {
                                printf("\b\n");
                                j=j+1;
                            }
                        }

                    }