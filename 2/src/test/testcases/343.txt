main: function void() {
                        foo(2 + x, 4.0 / y);
                        goo();
                          for (i[0] = 1, i[0] < 10, i[0]+1) {
                            print(":");
                          }
                    }